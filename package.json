{
  "name": "softwaretesting",
  "version": "1.0.0",
  "description": "softwareTesting",
  "main": "index.js",
  "scripts": {
    "test": "mocha",
    "test-cov": "nyc npm run mocha",
    "test-coveralls": "nyc npm run mocha && shx cat ./coverage/lcov.info | node ./bin/coveralls.js --verbose"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chai": "^5.1.2",
    "mocha": "^10.8.2",
    "nyc": "^15.1.0", 
    "coveralls": "^3.1.1" 
  },
  "nyc": {
    "reporter": [
      "lcov",
      "text-summary"
    ]
  }
}
